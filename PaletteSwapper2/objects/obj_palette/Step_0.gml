/// @description Insert description here
// You can write your code in this editor
if (obj_player.keyRotateL)
{
	targetAngle -= 360 / array_length(colorList);
}

if (obj_player.keyRotateR)
{
	targetAngle += 360 / array_length(colorList);
}